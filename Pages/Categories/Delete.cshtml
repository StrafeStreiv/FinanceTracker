@page "{id}"
@model FinanceTrackerWeb.Pages.Categories.DeleteModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Удаление категории";
}

<h2>@ViewData["Title"]</h2>

<h3>Вы уверены, что хотите удалить эту категорию?</h3>
<div>
    <h4>Информация о категории</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Название
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
        <dt class="col-sm-2">
            Тип
        </dt>
        <dd class="col-sm-10">
            @if (Model.Category.Type == "Income")
            {
                <span>Доход</span>
            }
            else
            {
                <span>Расход</span>
            }
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Category.Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-page="Index" class="btn btn-secondary">Отмена</a>
    </form>
</div>